/*
Assignment 02: Sum Mixed Types

Instruction:
- Implement `solution(a: i32, b: f64)` returning the sum as `f64`.

Requirements & guidance:
- Convert `a` to `f64` before adding.
- Keep precision in mind for large values.

Examples:
- `solution(2, 1.5) -> 3.5`
- `solution(-1, 2.0) -> 1.0`
*/

pub fn solution(a: i32, b: f64) -> f64 {
	unimplemented!()
}
