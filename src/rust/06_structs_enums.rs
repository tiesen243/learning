/*
Assignment 06: Structs and Enums

Instruction:
- Define a `Point`-like concept (or use primitive coords) and implement `solution(x1, y1, x2, y2)` that returns the Euclidean distance between two points.

Requirements & guidance:
- Signature: `pub fn solution(x1: f64, y1: f64, x2: f64, y2: f64) -> f64`.
- Use the distance formula: `sqrt((x2-x1)^2 + (y2-y1)^2)`. You may use `f64::hypot` or `(dx*dx + dy*dy).sqrt()`.
- Consider edge cases (identical points -> 0.0).

Examples:
- `solution(0.0, 0.0, 3.0, 4.0) -> 5.0`
*/

pub fn solution(x1: f64, y1: f64, x2: f64, y2: f64) -> f64 {
  unimplemented!()
}
